# SESSION_HANDOFF — superseded by SYSTEM_DESIGN.md, 2026-09-01

**The governing document is `docs/SYSTEM_DESIGN.md`.** Read it first; it is the settled, self-contained
system design (metric, control-flow, two-sign specification, the OTP substrate, the data pipeline, and the
build state). This file is now just the compaction-survival pointer to it.

## ⚠ READ THIS FIRST — the drift this file previously embodied
An earlier version of this handoff named the one next action as *"build the node-birth grower."* **That is
retired.** Engine A (growing mechanism nodes by population induction) was killed by the founder 2026-09-01.
The node set is **fixed and bounded** (the prior web); per-person growth is **dimensional** (discriminate by
adding a measurement dimension, Peitho), never nodal. If a post-compact summary steers you at "node birth,"
that summary is stale — trust `SYSTEM_DESIGN.md` and the git log, not the summary.

## WHERE WE ARE (see SYSTEM_DESIGN.md §12 for the authoritative version)
- **Built (positive-sign, 106 tests green):** `web.py` (the fixed weighted-ternary prior web + `kill_matrix`),
  `search.eliminate_to_survivor` / `loop.resolve_presentation` (seedless σ-trajectory, H→0), `ground.py`
  (the SymbolicSpellCheck front door). Statistical genus ripped.
- **This session:** worked the full design through end to end and wrote `SYSTEM_DESIGN.md` (the governing
  doc); reconciled `CLAUDE.md`, `AGENTS.md`, and the theory docs to it.

## THE NEXT ACTIONS (SYSTEM_DESIGN.md §12, in priority order)
1. **The negative-sign censor layer (μ⁻)** — physics-orthogonal role exclusions (GO/Pfam/Reactome) as
   zero-tax censors + treatment-response as directional censors; certified-⊥ for "no disease"; score
   σ(P, μ ∪ μ⁻).
2. **The OTP positioning core** — port Peitho `position.py` (`deviation_position` / `signature` /
   `discriminates`) as the per-person elimination layer; reconcile with `web.py` reachability.
3. **The discrimination-dimension selector (Jeeves)** — the `STUCK`-branch: rank unmeasured dimensions by
   EIG (positive *and* negative), return the highest-value question.
4. **The web-build pipeline** — repurpose `harmonizing` + wiki-ingest + BLAST over UniProt/GO/Reactome.
5. **The LRRK2 positive control** (canon §13.3) — recover the LRRK2–NOD2–RIPK2 shadow as coherence, blind.

## RECONSTRUCTION TEST
From `SYSTEM_DESIGN.md` alone a fresh session should recover: disease = shadow; one clinical engine over a
prior web (Engine A retired); two-sign specification (positive elimination + negative censors, certified-⊥);
non-idempotence = γ = bridges, completeness by κ-coverage not count; direction as negative-sign censor-shadow;
d bounded by proven gene categories; biology as an OTP geometry. If it cannot, re-read `SYSTEM_DESIGN.md` and
the git log before doing anything.
