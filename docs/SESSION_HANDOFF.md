# Session handoff — Homeostat, 2026-08-30 (post-realization, pre-rebuild)

*Read the auto-loaded `CLAUDE.md` and `AGENTS.md` FIRST — they were rewritten this
session and they carry the load-bearing correction. This handoff is a pointer, not an
authority; the theory is `docs/THEORY_OF_THE_CASE.md` (rewritten) + the canon
`docs/REGULATORY_DEFICIT_PROGRAM.md` (untouched).*

## ★ THE REALIZATION THIS SESSION (the reason everything below is what it is)
The whole empirical arc — E/I/R PBS pile → generic interactome (STRING/GTEx) →
participation "bridges" → enrichment / pleiotropy / LRRK2 gates, and every
"hardening," "replication," and "make-PBS-load-bearing" step on top — was **statistics
substituted for data geometry.** It failed exhaustively (Part V of the theory doc), and
correctly, because the design was never statistics. The founder's actual conception:
the phenotype is a **combination of sub-threshold signals**; the loud statistical hits
**drown out** the quiet coherent etiologies; the method is **data geometry — finding
the information-theoretic coherence of a combination (entropy minima, κ,
improbable-AND-coherent)**, not frequency/association/enrichment/hub-score. Population
genetics is a *search-order prior* (one input), diagnosis is a *story* (Winston), and
Ayurveda is a *decorrelated carving-source*, never an authority.

## ★ THE ONE NEXT ACTION (post-compact, WITH the founder — conversational first)
**Do NOT resume the statistical pipeline. Design the data-geometry method layer with
the founder, on a conversational register, before writing code.** The founder said
explicitly: rip out the giant chunks that aren't wiring, rebuild from scratch, using
the session discipline. So:
1. Re-read `docs/THEORY_OF_THE_CASE.md` Parts I–III (conception, deferred method SHAPE,
   keep-vs-rip inventory) and `~/Projects/Kaggle_Killer/DATA_GEOMETRY_ARCHITECTURE.md`
   (the founder's actual architecture: OTP, Informational Zero, COEC, GSE, HDC).
2. Restate the mechanism in your own words per the AGENTS.md gate — the test is *where
   does the coherence-of-a-combination live, and where would I be substituting a
   statistic?* Have the founder confirm.
3. Then, and only then, design the method (the object = data-as-geometry containing the
   phenotype; the signal = coherence of a combination; PBS/traditions = priors). This is
   the deferred Part II, to be reasoned out together — do NOT author it solo.

## ★ RESOLVED — do NOT reopen (with reasons)
1. **The method is data geometry, not statistics.** RESOLVED, load-bearing (CLAUDE.md
   LAW 1). A statistic is at most a cheap prior.
2. **The E/I/R PBS pile is a search-order prior, not the candidate object.** The old
   CLAUDE.md said otherwise; that was the poison. RESOLVED (§7).
3. **The generic interactome (STRING/GTEx) + participation is not the mechanism** — it
   is phenotype-free topology; the gnomAD replication proved the population signal was
   inert in it. RESOLVED (Part V, Act 2).
4. **Statistics-as-validator failed** — §8.4 enrichment died under LD (p 0.985); the
   §3.2 pleiotropy "pass" measured the generic graph; making PBS load-bearing broke the
   gates 0/8. RESOLVED as pathology (Part V), not design.

## ★ FORBIDDEN (with why — the drift attractors)
- **Reaching for ANY statistic as the method or the significance** — frequency,
  association, enrichment, population-differentiation rank, network hub/participation,
  p-value, pleiotropy count. WHY: this is THE drift, repeated 4× on 2026-08-30; it wears
  the costume of rigor. (CLAUDE.md LAW 1/2 auto-loads.)
- **Running `annotation-recovery`, `eir-enrich*`, `pbs-restricted`, `sig-descent`, or
  the participation `bridge-discovery`/`lrrk2-gate` as "the method."** WHY: they ARE the
  Part V pathology; they live in git history as a record, not a design. Do not "resume"
  them.
- **Building on the generic interactome with no phenotype in the object.** WHY: Act 2.
- **Editing the canon** (`REGULATORY_DEFICIT_PROGRAM.md`) or authoring the method solo.

## ★ WHAT IS KEPT (wiring / substrate the rebuild stands on)
`pbs.py` (prior math), ingestion (`eir_cohort.py`, `gnomad_pile.py`, the resumable
downloads, `HOMEOSTAT_TAG` cohort-namespacing), `kappa.py` (the κ engine — the one
genuinely data-geometry piece, transported from Regenesis), GRCh37 reference data, the
lint/type/test/gate discipline. See `docs/THEORY_OF_THE_CASE.md` Part III.

## ★ RECONSTRUCTION TEST
From this alone recover: (a) the method is data-geometry coherence of sub-threshold
combinations, NOT statistics; (b) the next action is a *conversational* method-design
with the founder, not resuming any pipeline; (c) forbidden = any statistic as the
method, the four Part-V pipelines as "design," the generic interactome as the object;
(d) what's kept = ingestion + `pbs.py` (as prior) + `kappa.py` + discipline. If any is
unclear, read `CLAUDE.md` + `AGENTS.md` + `docs/THEORY_OF_THE_CASE.md` Part V, then
`git log --oneline`.

## ★ SOURCES (can contradict this summary — read on doubt)
- `CLAUDE.md`, `AGENTS.md` — rewritten this session; the auto-loaded guardrails. READ FIRST.
- `docs/THEORY_OF_THE_CASE.md` (rewritten) + `docs/REGULATORY_DEFICIT_PROGRAM.md` (canon).
- `~/Projects/Kaggle_Killer/DATA_GEOMETRY_ARCHITECTURE.md` + `competitions/RSNA_Knee/` — the
  apparatus this was ported from.
- `docs/runs/2026-08-30-*` — the pathology, preregistered and committed (the four probes).
- `git log --oneline` — latest guardrail commit `e5d55a6`.
