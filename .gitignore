# Data — never committed (population downloads are huge; personal genotype data stays out of git)
data/
*.vcf
*.vcf.gz
coverage.xml
.coverage

# Secrets / local environment — never commit credentials
.env
.env.*

# Maintainer's local agent dev-discipline — kept private, not tracked in the public repo
CLAUDE.md

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
build/
dist/
.ruff_cache/
.pytest_cache/

# OS
.DS_Store
._*

# Detective mutation-testing reports (build artifacts)
.detective/
.wesker/

# Serena language-server project cache (build artifact)
.serena/

# Prism setup-health state cache (build artifact)
.prism/
