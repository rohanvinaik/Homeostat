# A confirmed mechanism component: the SAME gene-pair witnessed by two INDEPENDENT networks (convergence
# = H3, orthogonal partials that SUM). Literal single subject + object both sides -> fires (contract §3).
if x amplifies y and x binds y then x becomes component
if x inhibits y and x binds y then x becomes component
if x amplifies y and x channels y then x becomes component
if x binds y and x channels y then x becomes component
